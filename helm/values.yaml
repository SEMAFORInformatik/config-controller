# Default values for config-controller

image:
  repository: ghcr.io/semaforinformatik/config-controller
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

affinity: {}

nameOverride: ""
fullnameOverride: ""

imageCredentials:
  registry: ghcr.io
  username:
  password:

otel:
  enabled: false
  endpoint: "http://tempo.grafana:4318/v1/traces"
